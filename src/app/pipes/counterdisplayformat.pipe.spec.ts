import { CounterdisplayformatPipe } from './counterdisplayformat.pipe';

describe('CounterdisplayformatPipe', () => {
  it('create an instance', () => {
    const pipe = new CounterdisplayformatPipe();
    expect(pipe).toBeTruthy();
  });
});
